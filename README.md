# OpenDSS

OpenDSS is the clean active codebase for the Open Visual Droplet Sorter desktop runtime.

This repo was created from the accepted runtime state of `C:\Users\goals\Codex\CNN for Droplet Sorting` on 2026-05-14. Historical worker reports, orchestration logs, screenshots, and bulky evidence are intentionally kept outside this repo in `C:\Users\goals\Codex\OpenDSS\1. Agent work space - archieve`.

## Contents

- `app/runtime/` - active Qt Widgets/C++ runtime, model registry, packaging scripts, and installer surface.
- `app/runtime/desktop_app/` - active desktop application target.
- `app/runtime/models/` - packaged runtime models and metadata.
- `docs/` - concise current-state, build, runtime, training, migration, and decision notes.

Python training is deferred from this first clean cut. See `docs/training.md`.
