# DBG-008 / DBG-009 / DBG-013 final v2 installer evidence

Date: 2026-07-30

## Scope

- `DBG-008`: installer version derives from the v2 application's version.
- `DBG-009`: Training provisioning opens a visible raw-progress console.
- `DBG-013`: launching the v2 application does not create an application console.

## Corrected findings

- The earlier installer fixture under `C:\b\odss-debug-lead-hil\DBG-008-009-installer-final` used a legacy application build (`BUILD_QT_GUI=ON`, `BUILD_QT_GUI_V2=OFF`). It is superseded and is not acceptance evidence.
- The first visible-progress source concatenated a stray quote after `-ComputeProfile Auto`, merging `-CheckOutput` into the profile value. The corrected installer produces separate `-ComputeProfile`, `Auto`, `-CheckOutput`, and path arguments.
- The v2 CMake target did not explicitly request the Windows GUI subsystem. It now uses `qt_add_executable(... WIN32)`.
- Package validation now rejects `OpenDSS.exe` unless its PE optional-header subsystem is `2`.
- Packaging can receive an explicit qualified prepared-dataset root instead of requiring ignored datasets inside every worktree.

## Actual v2 build

- Build root: `C:\b\d13`.
- Configuration: Release.
- `BUILD_QT_GUI=OFF`.
- `BUILD_QT_GUI_V2=ON`.
- Build result: pass.
- Qualified runtime deployment target: pass.
- Accepted v2 executable:
  `C:\b\d13\Desktop_app_v2\Release\Desktop_app_v2App.exe`.
- SHA-256:
  `1C0463C8472559A91DF68C07CA3BE4D2932699EAE013EA6DB6BC62153DB3B8EB`.
- PE subsystem: `2` (`Windows GUI`).
- Qualified ONNX Runtime SHA-256:
  `A64BDD69D14F3685142C34FF46546A98CDD9CCAE6130619BBEE7414B5DD83B1B`.

## Package and installer

- Portable package:
  `C:\b\d13\portable\OpenDSS_20260730_234901`.
- Packaged `OpenDSS.exe` SHA-256:
  `1C0463C8472559A91DF68C07CA3BE4D2932699EAE013EA6DB6BC62153DB3B8EB`.
- Package-manifest GUI-subsystem check: pass, subsystem `2`.
- Packaged provisioner SHA-256:
  `EAE5F0218FA38F11DCF4057EA0FD311B522D946E09BD6677B975E0BFE29180F6`.
- Installer:
  `C:\b\d13\installer\OpenDSSSetup.exe`.
- Installer ProductVersion: `2.0`.
- Installer SHA-256:
  `342C401E97358F57B0903A2666869D62B9506345669BD14574C6122B35DB455E`.
- Installer evidence:
  `C:\b\d13\installer\installer_evidence.json`.
- Complete preflight:
  `C:\b\d13\DBG-008-009-013-preflight-final.json`.
- Complete preflight result: `pass`, zero errors, zero remaining inputs.
- Build log: `C:\b\d13\build-installer.log`.
- Fresh final Plan Guardian: `PASS`.

## Verification boundary

- The actual v2 Release build, qualified deployment, package validation, Inno compilation, installer metadata, executable identity, and GUI PE subsystem are verified.
- CTest was not repeated because this batch changes installer/provisioning scripts and the executable's Windows subsystem classification; the preceding integrated production test gate remains 53/53.
- Real-installer HIL remains for:
  - observing the `OpenDSS Training Setup` console through successful and failed provisioning;
  - proving the clean installer replaces the stale temporary Start Menu shortcut;
  - launching installed v2 OpenDSS from the new shortcut and confirming no console window appears.

## Rollback

- Revert the v2 `WIN32` target flag, GUI-subsystem package guard, corrected provisioner arguments, visible progress messages/window, application-derived installer version, and explicit prepared-dataset-root plumbing.
- No qualified detector, camera, DAQ, ONNX inference, trainer, model, dataset content, or persistence behavior was modified.
