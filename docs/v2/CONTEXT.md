# OpenDSS v2 implementation context

## Authority

The [Consolidated Product Design Specification](design/consolidated-design-draft.md) (`ODSS-DES-002`) is the single master product and design specification.

- The current slice limits authorized work but cannot override the master.
- Other canonical documents, amendments, reviews, and repository behavior are provenance or evidence only.
- If anything deviates, follow the master. If the master is ambiguous, internally inconsistent, missing, or unsafe to follow, stop and clarify with the user.
- Verify the pinned master hash with [verify-consolidated-design-lock.ps1](design/verify-consolidated-design-lock.ps1) and read only the exact applicable sections.
- [Archived historical design evidence](archive/product-design-draft-v0.1.md) and repository code remain evidence only.

Derived engineering documents, including this file, cannot override `ODSS-DES-002`. Report ambiguity or conflict rather than choosing a convenient interpretation.

## Documentation map

| Topic | Authoritative document | Relevant heading or section | Derived engineering document, when useful |
|---|---|---|---|
| Product scope and exclusions | [Approved Product Model](canonical/product-model.md) | §2 Product definition; §17 First-release boundaries | — |
| User-approved visual review | [UI/UX Visual Review Amendment — 2026-07-23](OpenDSS_v2_UIUX_Visual_Review_Amendment_2026-07-23.md) | All decisions; scoped override for listed visual topics | Current Slice |
| Approved UI/UX amendment | [Approved UI/UX Design Amendment](OpenDSS_v2_UIUX_Design_Amendment.md) | §§2–19 | All downstream documents listed below |
| Navigation and workspace behavior | [Information Architecture and Screen Inventory](canonical/information-architecture.md) | §1 Application shell; §2 Navigation hierarchy; §3 Complete workspace inventory; §4 Workspace state inventory | — |
| Application state and resource locks | [Interaction and Application-State Specification](canonical/interaction-and-state.md) | §3 Conceptual application-state model; §17 Shared resource ownership and navigation effects | — |
| Fixed versus editable configuration | [Approved Product Model](canonical/product-model.md) | §11 Editable and fixed configuration; also IA §1.6 Bottom Hardware panel | — |
| Detector behavior | [Approved Product Model](canonical/product-model.md) plus nonconflicting [Detailed Workflow Specification](canonical/detailed-workflows.md) | Product Model §7.1, §7.8, and §11.3; Workflow §13.6 Detection and crop rule | [Reusable-core audit](audits/reusable-core-audit.md), findings F-A01 and F-A02 |
| Protected reusable assets | [Repository agent policy](../../AGENTS.md) | Protected reusable technical assets; Change-control rule for protected assets | [Reusable-core audit](audits/reusable-core-audit.md) |
| Persistence/threading requirements | [Approved Product Model](canonical/product-model.md) plus nonconflicting [Detailed Workflow Specification](canonical/detailed-workflows.md) | Product Model §15 Persistence and provenance; Workflow §31 Event persistence | [Reusable-core audit](audits/reusable-core-audit.md), especially F-E02; [implementation plan](implementation/reuse-first-plan.md), P0-2 |
| Qt Design Studio handoff | [Consolidated Product Design Specification](design/consolidated-design-draft.md), subordinate and under review | §24 Qt Design Studio and design-handoff contract | [Qt Design Studio adoption record](implementation/qt-design-studio-adoption.md) |
| Current design-review gate | Canonical sources above | Page-by-page visual interpretation | [Page Composition Interpretation](reviews/page-composition-review.md) and [Current Slice](implementation/current-slice.md) |
| Future implementation sequence | — | Engineering guidance only | [Reuse-First Implementation Plan](implementation/reuse-first-plan.md) |
| First two visual rounds | Current slice and canonical sources above | Visual scaffold and frozen form/runtime seam | [Visual Scaffold Two-Round Plan](implementation/visual-scaffold-two-round-plan.md) |

## Reading workflow

1. Read [AGENTS.md](../../AGENTS.md).
2. Read this file.
3. Read [current-slice.md](implementation/current-slice.md).
4. Open only the canonical sections referenced by the current slice.
5. Open a complete specification only when the task spans it or a conflict is unclear.

## Current status

- `UAT-CONFIG-001` software validation is FINAL PASS/CLOSED. Detector characterization passed in 0.27 s and `live_sorting_controller_test` in 1.62 s. The single authorized diagnostic `ShellSingleImage` rerun at `4ca4eeb` passed 50/0 in 1.83 s; the rounded source-area `smallDroplet` check passed; result SHA is `2C49E7F4…D733`; and the audit found zero relevant residual processes, windows, dialogs, or crashes. Do not repeat software or standalone GUI validation. The frozen-candidate `FINAL-UAT-001` pass exercises visible Configuration controls once. Product authority remains unchanged: visible `Configuration`, sole `Small-droplet rejection`, exact `100 px²` initial/legacy value, nearest-pixel rectangle dimensions, profile-only current-value persistence, immediate active-Run application, and no Run/event/log/Results provenance.
- `UAT-ROUTE-HIL-001` DAQ-arbitration acceptance is retracted by `UAT-DAQ-004` because it depended on superseded final-observed-route gating. Preserve without repetition only its waveform/flat-zero, Stop, pause, fault/teardown, scalar-zero, app-close, and zero-process safety evidence. Its Waste/`not_requested` and equality/no-callback artifacts remain factual regression evidence, not acceptance of Decision-to-DAQ semantics.
- `UAT-DAQ-004` authority is reconciled in `ODSS-DES-002` §§15.6–15.7 and 16.4: Decision Boundary/final-Y and Observed Route are observer/comparison-only and never control DAQ; Every Droplet OFF makes model Decision control DAQ; ON makes every accepted droplet Decision Hit and trigger when DAQ is enabled; Observed Route persists only for comparison. Reproduced blocker `…\Sequence Test - 20260226_163405_BEST SO FAR-8\events.csv`, SHA `D4515CEB...5A0A`, records 197 Decision Hit, 197 observed Waste, and 197 `not_requested` under Every Droplet plus DAQ enabled. No GUI/backend/Dataset launch until a scoped reviewed correction and frozen candidate exist.
- `UAT-REJECT-001` is queued after the Training freeze. Normal Live/Sequence Run CSV rows write integer `rejected=0`; undersized rows write `rejected=1` and produce no crop, inference, Decision, Observed Route, or DAQ output even with Trigger Every Droplet ON. Rejected is counted separately and excluded from Total Droplets and all model/Decision/Route counts; no `rejection_reason` field/column exists. `UAT-RUNS-001` and `UAT-RUNS-002` are FINAL PASS/CLOSE at integrated and pushed commit `4625e28`: accepted behavior includes right-panel scrolling, effective Live+Sequence root aggregation/deduplication, confirm/cancel native Recycle Bin removal, identity-replacement guarding, and active-run plus containment safety. Repository/removal/Shell focused evidence and scoped rereview passed. `UAT-RUNS-DESIGN-001` remains a separate nonblocking visual-improvement request with no inferred design.
- `UAT-REJECT-001-PROT-MULTI` amends the RELEASED `UAT-REJECT-001-PROT` seam: keep the one accepted `FastEventResult` and its accepted geometry/tracking outcome unchanged, and add alongside it one ordered zero-or-more per-frame collection of facts for every otherwise-valid candidate rejected solely for below-minimum source-pixel area. The collection crosses only the existing `FastEventResult` → adapter → `DropletDetectionFrame` → Live/Sequence service path. Each item carries only existing source/event identity and measured source-pixel area; it has no image/crop, inference, Decision, Observed Route, DAQ, or track lifecycle. Use the smallest ordinary reusable C++ collection/buffer with zero steady-state per-frame heap allocation and bounded candidate iteration; no callback framework, new service, registry, schema expansion, abstraction, or per-consumer duplicate is authorized. The exact unchanged write set and gates are recorded in the current-slice ledger. Status is RELEASED after this documentation commit; the existing worker remains stopped until this commit is integrated, and its preserved diff remains unchanged.
- `ENV-ONNX-DLL-001` is RESOLVED/CLOSED as a configuration-root diagnosis, not a qualification waiver or substitution. Reject defaulted to stale `C:\onnxruntime-gpu` v1.23 DLL SHA `C602...82C`; accepted builds use the existing qualified root `C:\Users\goals\AppData\Local\Temp\opendss-ort-1.25.1-cuda13\extracted\onnxruntime-win-x64-gpu-1.25.1`, whose headers/import library and DLL SHA `A64BDD69...83B1B` match the repository lock. Future Reject configure must pass that exact `ONNXRUNTIME_DIR`. No runtime file or hash changed, and no requalification or repeat is required.
- `UAT-MODEL-LIB-001` is queued after corrected-candidate Training freeze acceptance. Library owns Add Model and Import Model. Add Model popup requires nonblank unique Name, MobileNetV3-Small or EfficientNet-B0, and approved Starting Weights. Train selects that Library-defined identity/architecture/initialization read-only, plus Dataset, Compute Device, and Output Location. Retraining always creates a new named Library entry/package atomically on success and never overwrites/mutates the source. Import accepts only a complete package selected via `metadata.json`; the documented repo-only converter supports only the two approved architectures. Remove Model is adjacent to Import Model and moves the confirmed OpenDSS-owned complete package folder to Windows Recycle Bin. Active Model, in-use, registry, and package-integrity locks remain. DESIGN exact Library/Train form ownership follows seam inventory; FUNCTIONAL then owns create/import/remove/recycle/locks and Train consumption/publication, followed by integration. Current authority names no exact form file.
- `UAT-PATH-001` is queued after corrected-candidate Training freeze acceptance. Existing Capture output selectors default nonblank to `%USERPROFILE%\Documents\OpenDropletSortingSuite\datasets`; Train/model creation/export to `models`; Model Test to `reports`; Live/Sequence Test to `runs`. Valid selections persist per output selector/action; invalid persisted paths visibly fall back to the corresponding standard root; existing unique timestamped/name subfolders never overwrite. FUNCTIONAL owns the narrow default/persistence/fallback and existing-selector binding only.
- `UAT-PATH-001-RUNS` releases the controlling Results discovery interpretation. Results > Runs aggregates the current effective Live and Sequence Test run roots independently: each workspace's valid persisted root, otherwise that workspace's `%USERPROFILE%\Documents\OpenDropletSortingSuite\runs` fallback. Roots resolving to the same canonical folder are scanned once and Runs are deduplicated by canonical folder identity. No historical-root registry, recursive drive scan, migration, schema, or storage redesign is added. Existing Run validation/loading and active/in-use safety remain unchanged. FUNCTIONAL owns the existing preferences plus Runs controller/Shell bindings/tests; exact source writes await its seam inventory. Status is RELEASED after this documentation commit; no implementation is included here.
- `UAT-VIEWER-002` authority is published and one combined shared-form correction remains pending. In the one shared `FullSizeImageViewer`, each axis uses `AlwaysOn` exactly when that axis's content exceeds its viewport and `AlwaysOff` otherwise; both scrollbars are interactive and remain persistently visible/clickable throughout overflow. Normal wheel remains vertical, `Shift`+wheel horizontal, `Ctrl`+wheel pointer-centered at `0.3`–`10`, and pan plus all existing interfaces/geometry remain unchanged.
- `UAT-VIEWER-003` authority is published and joins the same pending form correction. The shared full-size image-viewer canvas/background is black for exactly Capture, Live, Sequence Test, Sequence Viewer, and Label selected crop. Only the former blue/navy canvas changes; surrounding panels, grids/thumbnails, placeholder text, overlays, and borders remain unchanged. DESIGN owns only `FullSizeImageViewerForm.ui.qml` for the combined `UAT-VIEWER-002`/`003` correction. FUNCTIONAL owns only policy assertions and one changed Shell rerun through the accepted harness after the `VAL-TEST-UI-005` execution hold is separately released. No GUI repeat, inference, interface change, geometry change, or per-consumer duplication is authorized. Neither decision is accepted until the pending correction and bounded validation complete.
- `VAL-TEST-UI-005` is a FATAL Viewer-only launch blocker. `C:\b\odss-uat-viewer-002-fix\Release` visibly produced `tst_ShellSingleImage.exe - Entry Point Not Found`. Containment is complete: the Viewer worker was interrupted and the exact build-root process/dialog/residual audit is zero. The Viewer Release contains no Qt DLLs; Shell CTest invokes the target directly and inherits ambient `PATH`, loading incompatible Qt/QML symbols, while the selected qualified product Release is Qt 6.11.1. All Viewer executable/test launches are globally held. FUNCTIONAL may make only a static central-harness/CMake registration correction that supplies a sanitized `PATH` with the exact target directory, matching Qt 6.11.1 `bin`, and System32/SystemRoot; performs `qoffscreen` plus required Qt DLL/plugin preflight; and preserves the existing offscreen/QML environment and stable working directory. No execution is permitted until changed-lines static review and a separate VALIDATION release. Unrelated non-launching lanes may continue.
- `SCHED-UAT-002`: do not interrupt the corrected-candidate `VAL-TRAIN-CU-001` freeze. After its frozen handoff, release maximum safe nonoverlapping lanes for (1) critical DAQ/route FUNCTIONAL correction, never delayed by visual polish; (2) rejection CSV/detector persistence; (3) Runs scroll/remove plus DESIGN→FUNCTIONAL visual seam; (4) shared Viewer conditional DESIGN→FUNCTIONAL runtime; (5) Model Library/Train DESIGN→FUNCTIONAL workflow; and (6) output defaults FUNCTIONAL. Use separate worktrees/build roots, no overlapping protected forms/files, and single-writer integration in dependency order; exact files await seam inventory.
- `UAT-ROUTE-003-PROT` releases the protected observer-only correction recorded in the current-slice ledger. Live and Sequence Test currently finalize their pending event on the first `!detected` frame even though the qualified Fast Event Detector retains the track until `resetFrames=2`. The smallest existing detector-result seam may expose only a factual lifecycle-ended indication so those two services finalize after the second miss and retain the latest detected centroid Y across a one-frame miss/reappearance. Their existing controller/service seams may replace the current workspace-local Decision Boundary X/Y/side thread-safely while Running so subsequent lifecycle finalizations use it immediately. Preserve detector, centroid, reset, crop, inference, performance, persistence, and DAQ mechanics; boundary state remains nonpersistent and observer-only with zero DAQ effect. FUNCTIONAL owns the bounded correction and VALIDATION owns changed-seam acceptance. Status is RELEASED after this documentation commit; no implementation is included here.
- `UAT-VIEWER-001` is accepted on integrated candidate `7fa5dd0`: exactly five full-size consumers—Capture, Live, Sequence Test, Sequence Viewer, and Label selected crop—use the single shared runtime viewer. Grids and thumbnails remain excluded. Sequence Viewer controls and `Ctrl`+wheel both clamp `0.3`–`10`. The durable offscreen `ShellSingleImage` test passed 1/1 in 1.97 s and found exactly one shared viewer per consumer; unchanged geometry, pointer-centered zoom, and pan evidence was reused without Computer Use or repeated GUI validation.
- `UAT-TRAIN-001` current-machine Training backend lane is closed. The authoritative environment check for `%LOCALAPPDATA%\OpenDSS\training-venv-gpu` and registered controller/protocol tests remain accepted without repetition. Registered real `TrainingService` probe `3768e22` passed in 10.61 s using the exact OpenDSS Python command `-I -m droplet_trainer train`, CUDA, a genuine training stage, and bounded cancellation to `Interrupted`. Its nine PNG files plus manifest were preserved; registry SHA `6E4251…3B95` and ImageNet SHA `047DCF…4E1F` were unchanged; and the audit found zero relevant residual processes, dialogs, or crashes. Fresh-machine installer qualification remains deferred.
- `UAT-TRAIN-INSTALL-001` authorizes installer setup, preflight, and dry-run infrastructure to finish in parallel with the program lanes while the user is away. Do not create redundant large packages or visibly launch the product. The final stamped installer remains blocked until the user accepts the almost-final executable, and fresh-machine qualification remains deferred.
- `MODEL-WHEEL-UPDATE-003` installed the exact accepted trainer wheel SHA-256 `20e070bf0e5a114bf9daadaec22ff81a704c754d8077a7cea7af93d9dddab796` successfully at accepted updater commit `824c549`, exit 0. Installed bytes match all 14 production modules including `model_test_batch_backend`; the 37-distribution closure passed; and zero snapshot, temporary diagnostic, updater, pip, or Python residue remains. Installed hashes are METADATA `a4994de4…92f3`, RECORD `30e1abaf…5ff6`, and entry point `6f8ecbfa…8935`; command-log SHA `b988cd3a…77086` is authoritative. The exactly-once installed production gate then passed under sanitized/offline conditions using exact OpenDSS Python `-I -m droplet_trainer model-test-process --help`, exit 0, importing the production batch backend and exposing the checkpoint surface without Dataset, model, GUI, or network access. Python SHA `0b471133…fc14`, output SHA `d5fd511c…d5a2`, zero Python/pip residue. Do not rerun the updater or CLI gate. `VAL-MODEL-005-REP` dependencies are closed but its execution still requires separate Validation release.
- `MODEL-WHEEL-SUCCESS-LOG-001` is FINAL STATIC ACCEPT/CLOSED at integrated `4982383`. The post-commit catch emits one CR/LF-collapsed stderr line with exception type/message capped at 256/1024 characters; nested stderr failure is swallowed. Parser/diff checks pass and success/exit 0, transaction, and replacement behavior are unchanged. No updater/live execution occurred or is required. The prior stale failure JSON remains non-authoritative.
- `VAL-MODEL-005-REP` changed-lines STATIC PASS at `c4f1e47` under `ODSS-DES-002` §§13.3 and 13.5–13.7; the six-record probe is frozen. It closes the four prior seams and explicitly fails on any sorted `*.partial*` or `model_test_checkpoint.json` residue under `outputRoot` before cleanup. No further probe or registration change is requested or authorized.
- `VAL-MODEL-005-REP` is PASS/CLOSED at corrected `9965f44`: registered sanitized/offline non-GUI CTest 1/1 in 9.78 s; six manifest-order facts, two per class; effective CUDA/no fallback; one durable batch in 8,334 ms; seven prediction lines; coherent v3 checkpoint SHA `9B247907…96D46` plus TEMP metadata SHA `0CA3FCBD…854AF`; no ONNX attribution or partials; all source/registry/model-package hashes unchanged; TEMP removed; zero probe/Python/pip residue. Log SHA `606a3536…48ab`. Do not repeat.
- All Model Test, visible UAT, window, and unrelated HIL gates remain accepted without repetition. Only the superseded final-observed-route DAQ-arbitration acceptance is retracted.
- `FINAL-UAT-001` preserves unchanged visible-control/layout, `UX-WINDOW-003`, nonblank retention/no-flicker, and unrelated HIL evidence. Its corrected-candidate gate is reopened only for `UAT-DAQ-004`; Validation remains idle until the authority lock is integrated, FUNCTIONAL supplies the scoped reviewed correction, and a frozen corrected candidate is released.
- `SCHED-001`: release is frozen by `UAT-DAQ-004`; the final installer stamp remains blocked. Preserve accepted unrelated evidence without repetition. `UAT-ROUTE-003` and `UAT-RUNS-DESIGN-001` are not DAQ-release gates; final stamping still requires explicit user acceptance and fresh-machine qualification remains deferred.
- `UAT-MODEL-005` authorizes automatic high-throughput Python/PyTorch batching for the existing visibly named `Model Test` operation without changing its scientific semantics, visible workflow, metrics, or recovery contract. Input remains one structured, labeled OpenDSS Dataset; arbitrary image-file/folder input is not added. Training and internal Dataset Validation processing use the local Python/PyTorch backend at `%LOCALAPPDATA%\OpenDSS\training-venv-gpu`, automatic GPU-first with CPU fallback; they do not use C++ ONNX Runtime. Model Package metadata declares `checkpoint_sha256`; new `opendss.model_test.v3` summaries record executed checkpoint SHA-256 plus metadata SHA-256, while v2 summaries remain readable. Trusted legacy packages missing the declaration migrate automatically and atomically; mismatches or migration failure block execution without new UI/action. Live and Sequence retain ONNX+metadata provenance. The runtime selects the largest qualified memory-safe batch automatically; no user batch setting exists. Per-image facts/order are preserved, completed batches are atomic persistence checkpoints, Stop begins no later batch, and allocation failure reduces batch size before truthful failure.
- `UX-WINDOW-002` supersedes only the no-restore/no-fixed-minimum portion of `UX-WINDOW-001`. OpenDSS still starts maximized. Restore Down is supported; the restored window uses the existing Qt native/logical clamp to enforce a 1600 × 900 minimum, may grow larger, and never resizes below it. No separate physical-pixel interpretation or acceptance gate is required.
- The user-approved [July 23, 2026 visual-review amendment](OpenDSS_v2_UIUX_Visual_Review_Amendment_2026-07-23.md) remains authoritative for its listed visual topics. Its visual/mock baseline was accepted by the user on July 23, 2026; remaining appearance and graphics refinements continue in a separate design thread.
- The accepted visual navigation scaffold and Mock Single Image slice is closed. Its forms, exported aliases, and frozen seam are the functional contract by default. A design change requires exact named form ownership and an alias/interface handoff before functional integration.
- The current slice is [production stabilization and integration](implementation/current-slice.md) on `codex/functional-orchestrator`, based at `1e80f3b`. The user explicitly authorized completing the recovered GUI/controller stabilization diff, focused fixes, independent review, commit, push, and artifact validation; this supersedes the former headless-only boundary for this slice.
- `FI-TEST-001` is green: the final Qt 6.11.1 MSVC Release GUI links, thirteen affected C++ targets pass 13/13, and the QML shell suite passes 43/43. `FI-MODEL-001` independently loads the trusted installed EfficientNet model without network fallback and processes exactly 2,103 frames with DAQ off.
- `CR-001` is corrected and independently accepted: `OperationCoordinator` serializes callback invocation against destruction, with a focused 1,000-iteration concurrent release/destruction test.
- The release candidate executable is `C:\b\odss-v2-fpath-8435\Release\Desktop_app_v2App.exe`. `FI-REVIEW-001` is accepted and Graphify is refreshed. Validation returned and Integration corrected a production camera-identity fallback and a re-entrant continuous-DAQ Stop lock; focused QML/DAQ tests and scoped rereview accept both corrections, including exact task Stop/clear and scalar-zero evidence for Stop and exit.
- Full-size-only Validation passed factual `DCAM:0` identity, visible preview frames during real streaming, continuous `Dev1/ao0` Start/Stop/exit at 5 Vpp and 10 kHz, exact-zero teardown evidence, and truthful disabling of the competing finite-test action. The accepted candidate was exercised maximized at the full 1707 × 1019 available work area. Push of the reviewed correction and final immutable-hash acceptance are the remaining gates.
- The user granted standing hardware authorization. Validation owns announced Camera/DAQ HIL and the single consolidated `FINAL-UAT-001` pass on the frozen candidate; Integration remains the sole writer and corrects exact returned defects.
- The final approved structure records Label's ordered outer panel and Dataset Save As semantics, the shared 390 px outer-panel width/collapse behavior, Train and Model Test Results placement, and Settings Visuals/Text Size policy. D-012 permits configured Datasets to switch between two and three classes while preserving label consistency; D-017 defines the sole application-wide Text Size preference.
- The canonical product, information-architecture, interaction-state, and detailed-workflow baselines incorporate the approved July 23, 2026 UI/UX amendment. The active [page-composition review](reviews/page-composition-review.md) remains a review artifact, not independent authority.
- A bounded work order naming exact writable files remains required before any source, form, wrapper, test, or CMake input is edited.
- Preserve qualified DCAM, NI-DAQmx, detector/crop, ONNX, trainer, export, and persistence mechanics. Do not rebuild datasets, weaken trusted metadata/checkpoint validation, or introduce production mock fallback.
