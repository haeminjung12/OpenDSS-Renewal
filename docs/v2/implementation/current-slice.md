# OpenDSS v2 functional slice — production stabilization and integration

## Status

**State:** Integration release candidate; independent artifact validation pending  
**Branch:** `codex/functional-orchestrator`  
**Base:** `1e80f3b`  
**Integration owner:** FUNCTIONAL orchestrator, sole writer

The user explicitly authorized completing the recovered production stabilization
diff, final GUI/controller wiring, focused fixes, build and runtime validation,
independent read-only review, commit, and push to `renewal/main`. This instruction
supersedes the former headless-only boundary for this slice. Visual redesign,
dataset rebuilding, model-integrity weakening, and unrelated refactoring remain
out of scope.

## Active coordination ledger

| ID | Owner | State | Requirement and evidence |
|---|---|---|---|
| `UX-WINDOW-002` | User / documentation authority; FUNCTIONAL artifact owner; VALIDATION acceptance owner | `UX-WINDOW-003` FINAL PASS/CLOSED on `4d4da434` | Supersedes only the no-restore/no-fixed-minimum portion of `UX-WINDOW-001`. OpenDSS starts maximized and Restore Down enforces the 1600 × 900 minimum. On executable SHA `6F2CB565AEAC2DBDC69F15FA94DA486CCDC2D13AF5792FC16DF97A5966095D64` at DPI 144, startup was maximized at 1721 × 1033; logical `SetWindowPos` 1200 × 700 clamped exactly to 1600 × 900; 1650 × 950 remained allowed; and real edge drag stopped at 1600 × 950. Process teardown was clean. Do not repeat. |
| `UAT-CONFIG-001` | FUNCTIONAL artifact owner; DESIGN protected-form owner; VALIDATION acceptance owner | SOFTWARE FINAL PASS/CLOSED | Detector characterization passed in 0.27 s and `live_sorting_controller_test` in 1.62 s. The single authorized diagnostic `ShellSingleImage` rerun at `4ca4eeb` passed 50/0 in 1.83 s; the rounded source-area `smallDroplet` check passed; result SHA is `2C49E7F4…D733`; and the audit found zero relevant residual processes, windows, dialogs, or crashes. Do not repeat software or standalone GUI validation. The frozen-candidate `FINAL-UAT-001` pass exercises visible Configuration controls once. Product behavior remains unchanged: visible `Configuration`; sole `Small-droplet rejection`; nearest-pixel mapping; exact `100 px²` initial/legacy value; profile-only current persistence; immediate active-Run effect; zero Run/event/log/Results provenance. |
| `UAT-ROUTE-002` | FUNCTIONAL artifact owner; DESIGN protected-form owner by exact work order | Route-semantic software acceptance superseded/retracted by `UAT-DAQ-004`; general safety evidence retained | The prior equality/Waste/resolved-Hit pulse assertions depended on Observed Route controlling DAQ and are no longer acceptance evidence. Preserve only the applicable `pulseMutex`, Stop/pause/fault no-late-output, and persistence-fault ordering evidence as general safety evidence. This documentation correction authorizes no test or GUI repetition. |
| `VAL-TEST-UI-003` | FUNCTIONAL correction owner; VALIDATION acceptance owner | Contained by accepted central harness `3ccdb29` | The prior ambient launch path remains prohibited. `sequence_test_controller_test` may run only through the shared accepted harness/preflight. |
| `VAL-TEST-UI-005` | FUNCTIONAL static correction owner; VALIDATION static/execution release owner | FATAL Viewer-only launch blocker; contained; all Viewer launches globally held | Visible blocker: `C:\b\odss-uat-viewer-002-fix\Release` produced `tst_ShellSingleImage.exe - Entry Point Not Found`. Containment is complete: the Viewer worker was interrupted and the exact build-root process, dialog, and residual audit is zero. Root cause is the Viewer Release containing no Qt DLLs while Shell CTest invokes the target directly and inherits ambient `PATH`, which loads incompatible Qt/QML symbols; the selected qualified product Release uses Qt 6.11.1. No Viewer executable or test may launch. FUNCTIONAL may make only a static CMake/central-harness registration correction: sanitize `PATH` to the exact target directory, the matching Qt 6.11.1 `bin`, and System32/SystemRoot; preflight `qoffscreen` and every required Qt DLL/plugin; preserve the existing offscreen/QML environment and stable working directory; and route the target through the central harness. No execution is authorized until changed-lines static review passes and VALIDATION separately releases execution. Unrelated non-launching lanes remain open. |
| `UAT-VIEWER-001` | FUNCTIONAL artifact owner; DESIGN protected-form owner | Validation accepted on integrated candidate `7fa5dd0` | Exactly five full-size consumers—Capture, Live, Sequence Test, Sequence Viewer, and Label selected crop—use one shared runtime viewer; grids/thumbnails are excluded. Sequence Viewer controls and `Ctrl`+wheel both clamp `0.3`–`10`. Durable offscreen `ShellSingleImage` passed 1/1 in 1.97 s and found one shared viewer per consumer. Unchanged geometry, pointer-centered zoom, and pan evidence was reused; no Computer Use or GUI repeat was needed. |
| `UAT-TRAIN-001` | FUNCTIONAL artifact owner | Current-machine environment gate passed; production workspace proof remains | Authoritative env-check for `%LOCALAPPDATA%\OpenDSS\training-venv-gpu` exited 0: Python 3.12.10; torch 2.10.0+cu130; torchvision 0.25.0+cu130; CUDA 13/RTX 4070; ORT-GPU 1.25.1 CUDA provider; writable output; no warnings. Evidence: `C:\Users\goals\AppData\Local\Temp\OpenDSS-validation-training-env-check`. Continue production discovery/controller/workspace proof. Fresh internet-connected Python-free-machine installer qualification remains deferred. |
| `VAL-TRAIN-UI-002` | FUNCTIONAL correction owner; VALIDATION acceptance owner | Superseded by broader `VAL-TRAIN-UI-004`; exact build root remains quarantined | Quarantine `C:\Users\goals\codex-builds\opendss-uat-train-current-host`: its `desktop_app\Release\OpenDSS.exe` (SHA prefix/suffix `C1241…835BA`) produced an Entry Point Not Found modal; Qt6Core/Gui 6.11.1 were present while Qt6Qml/Quick were absent, consistent with ambient runtime mixing. No residual process/window remains. Do not repeat the already-green authoritative Training env-check. |
| `VAL-TRAIN-UI-004` | FUNCTIONAL harness/correction owner; VALIDATION acceptance owner | Global stop narrowed by `VAL-TEST-HARNESS-001`; raw launches remain prohibited | The fatal repeat remains the reason every launch path must use the central harness. No raw PowerShell/`Start-Process` product launch is permitted. Every GUI-capable test/product verifier not yet registered remains blocked. |
| `VAL-TEST-HARNESS-001` | FUNCTIONAL harness owner; VALIDATION acceptance owner | PASS on commit `3ccdb29`; controlled reopen only | Static diff and clean-shell evidence accept the shared offscreen harness/preflight. Only its currently wired smoke path and `sequence_test_controller_test` may execute. Each additional GUI-capable test or product verifier requires registration through the same harness/preflight before lane-specific execution. This is not a blanket reopen. |
| `VAL-TEST-HARNESS-ROUTE` | FUNCTIONAL Route owner; VALIDATION acceptance owner | Route-semantic FINAL ACCEPT retracted by `UAT-DAQ-004`; harness/general safety evidence retained | The registered executions remain factual, but assertions coupling Observed Route to pulse requests no longer accept DAQ behavior. Preserve only the accepted harness operation plus applicable persistence-fault and concurrency/order safety evidence. No other path is authorized by this result. |
| `VAL-TEST-HARNESS-TRAIN` | FUNCTIONAL Training owner; VALIDATION acceptance owner | Execution PASS 2/2; focused controller/protocol gate closed | Registered `training_service_protocol_test` passed in 4.28 s and `training_controller_test` in 5.53 s, including self-hosted QCore fake-trainer child modes. Audit found zero relevant parent/child residual processes, windows, dialogs, or crashes. No real Python/backend/product executed. |
| `VAL-TRAIN-BACKEND-001` | FUNCTIONAL probe owner; VALIDATION acceptance owner | FINAL PASS/CLOSED on `3768e22` | Registered real `TrainingService` probe passed in 10.61 s using exact OpenDSS Python `-I -m droplet_trainer train`, CUDA, a genuine stage, and bounded cancel to `Interrupted`. Nine PNG files plus manifest were preserved; registry SHA `6E4251…3B95` and ImageNet SHA `047DCF…4E1F` remained unchanged. Audit found zero relevant residual processes, dialogs, or crashes. Environment/controller/protocol evidence was not repeated and no further current-machine Training backend execution is required. |
| `UAT-TRAIN-INSTALL-001` | FUNCTIONAL installer artifact owner; VALIDATION preflight acceptance owner | Setup/preflight/dry-run infrastructure authorized in parallel; final stamp blocked | Finish the smallest installer setup, preflight, and dry-run infrastructure while program lanes continue. Do not produce redundant large packaging or visibly launch the product. Do not stamp the final installer until the user accepts the almost-final executable. Fresh-machine qualification remains deferred. |
| `INSTALL-TRAINER-WHEEL-001` | User/documentation authority; FUNCTIONAL installer artifact owner; VALIDATION preflight acceptance owner | Authority resolved; FUNCTIONAL correction released after master/lock publication | Supersedes `ODSS-DES-002` §2.1.1's prior all-37-download wording. Bundle the exact accepted repository-owned OpenDSS `droplet_trainer` wheel, currently SHA-256 `76fba77831bf616b832b5dbc491bc29b97bf1ed0311ad0fe4c341a4d83002af3`, and verify its locked SHA-256 before installation. Download pinned CPython `3.12.10` and exactly the other 36 pinned, hash-locked third-party wheels. Preserve the exact 37-distribution environment closure, atomic visible failure, authoritative environment check, no runtime network fallback, and existing final-stamp/fresh-machine gates. No hosted trainer URL or signing input may be invented. FUNCTIONAL owns only the known smallest installer/catalog/provisioner correction; do not disturb the executable/PATH critical path. |
| `VAL-MODEL-005` | FUNCTIONAL Model Test owner; VALIDATION acceptance owner | Model Test FINAL PASS/CLOSED; combined-candidate release frozen by `UAT-DAQ-004` | Summary passed in 0.46 s, writer in 11.05 s, and registered service in 3 s at `0dd0bf3`. Accepted wheel installation, installed production CLI/backend, and the representative six-record CUDA Dataset gate are also PASS/CLOSED in their rows below. All Model Test gates are closed and must not repeat. Their acceptance does not release a combined candidate while `UAT-DAQ-004` remains a reproduced blocker. |
| `MODEL-WHEEL-LOCK-001` | Documentation authority owns accepted artifact identity; FUNCTIONAL preserves installed runtime; VALIDATION execution acceptance owner | Historical installed post-state and production CLI/backend PASS/CLOSED | Historical wheel SHA-256 `20e070bf0e5a114bf9daadaec22ff81a704c754d8077a7cea7af93d9dddab796` installed successfully at accepted updater commit `824c549`, exit 0; `INSTALL-TRAINER-WHEEL-001` supersedes it as the current package identity with locked SHA-256 `76fba77831bf616b832b5dbc491bc29b97bf1ed0311ad0fe4c341a4d83002af3`. The historical installed bytes matched all 14 production modules, including `model_test_batch_backend`; the 37-distribution closure passed; and no snapshot, temporary diagnostic, updater, pip, or Python residual remained. Historical installed evidence: METADATA `a4994de4…92f3`, RECORD `30e1abaf…5ff6`, entry point `6f8ecbfa…8935`, and command-log SHA `b988cd3a…77086`. The associated exactly-once sanitized/offline production gate passed using exact OpenDSS Python `-I -m droplet_trainer model-test-process --help`, exit 0; it imported the production batch backend and exposed the checkpoint surface without Dataset, model, GUI, or network access. Python SHA `0b471133…fc14`, output SHA `d5fd511c…d5a2`, zero Python/pip residue. Do not rerun the historical updater or gate. The best-effort final JSON replacement left the prior failure JSON stale; that JSON is not current-state evidence and is governed by `MODEL-WHEEL-SUCCESS-LOG-001`. |
| `MODEL-WHEEL-SUCCESS-LOG-001` | FUNCTIONAL diagnostics owner; VALIDATION static acceptance owner | FINAL STATIC ACCEPT/CLOSED at integrated `4982383`; no execution | The post-commit catch now emits one bounded stderr line with exception type/message capped at 256/1024 characters and CR/LF collapsed; a nested catch swallows stderr failure. Parser and changed-lines diff checks pass. Success/exit 0, transaction, and replacement behavior remain unchanged. No updater or live-environment execution occurred or is required. The prior stale failure JSON remains non-authoritative; accepted installed evidence is unchanged. |
| `VAL-MODEL-005-REP` | FUNCTIONAL corrected-probe execution owner; VALIDATION execution acceptance owner | Corrected exact-once PASS/CLOSED at `9965f44` | Controlling master: `ODSS-DES-002` §§13.3, 13.5–13.7. Registered sanitized/offline non-GUI CTest passed 1/1 in 9.78 s. It produced six manifest-order facts, two per class, using effective CUDA with no fallback; one durable batch completed in 8,334 ms and predictions contained seven lines. Provenance was coherent v3 with checkpoint SHA `9B247907…96D46`, TEMP metadata SHA `0CA3FCBD…854AF`, no ONNX attribution, and zero partials. All source, registry, model-package hashes remained unchanged; TEMP was removed; zero probe/Python/pip residue remained. Log SHA `606a3536…48ab`. Do not repeat. |
| `UAT-ROUTE-HIL-001` | FUNCTIONAL corrected-candidate owner; VALIDATION HIL acceptance owner | DAQ-arbitration acceptance retracted by `UAT-DAQ-004`; waveform/zero-safety evidence retained | The prior physical Waste/`not_requested` and equality/no-callback interpretation depended on superseded final-observed-route DAQ arbitration and no longer accepts DAQ semantics. Preserve the existing RIGOL flat-zero, Stop, pause, fault/teardown, scalar-zero, app-close, and zero-process safety evidence without repetition. The prior CSV and logs remain factual evidence of the regression, not acceptance of Decision-to-DAQ behavior. |
| `UAT-DAQ-004` | User / documentation authority; FUNCTIONAL correction owner; VALIDATION acceptance owner | Authority reconciled; reproduced implementation regression blocks frozen corrected candidate | `ODSS-DES-002` §§15.6–15.7 and 16.4 now match canonical `interaction-and-state.md` §0.4: Decision Boundary/final-Y and Observed Route are observer/comparison-only and never control DAQ. Trigger Every Droplet OFF makes the model Decision control DAQ; ON makes every accepted droplet Decision Hit and therefore trigger when DAQ is enabled. Persist Observed Route only for comparison. Reproduced evidence `…\Sequence Test - 20260226_163405_BEST SO FAR-8\events.csv`, SHA `D4515CEB...5A0A`: 197 model Decisions Hit, 197 observed Waste, and 197 `not_requested` with Every Droplet plus DAQ enabled. Known evidence sites are Sequence service 533–539, Live service 877, and writer/manifest validators 238–253; source remains untouched. No GUI/backend/Dataset launch until FUNCTIONAL corrects the exact regression, scoped review accepts it, and a frozen corrected candidate is released. |
| `UAT-REJECT-001` | User / documentation authority; FUNCTIONAL correction owner | Authority published; implementation queued after Training freeze | Under `ODSS-DES-002` §§15.12–15.13, 16.7, and 17.5, normal Live/Sequence Run CSV rows write integer `rejected=0`; undersized rows write `rejected=1`. An undersized detection produces no crop, inference, Decision, Observed Route, or DAQ output even with Trigger Every Droplet ON. Rejected is counted separately and excluded from Total Droplets and all model/Decision/Route counts. No `rejection_reason` field or column exists. |
| `UAT-REJECT-001-PROT` / `UAT-REJECT-001-PROT-MULTI` | FUNCTIONAL correction owner; VALIDATION changed-seam acceptance owner | RELEASED after this documentation commit; implementation pending; worker stopped until integration | This row releases only the smallest protected output seam needed by the existing `UAT-REJECT-001` semantics and amends it to surface **each** otherwise-valid undersized candidate. Keep the existing single accepted `FastEventResult` and its accepted geometry/tracking outcome unchanged. Alongside it, add one ordered zero-or-more per-frame rejected-candidate fact collection through the existing `FastEventResult` → adapter → `DropletDetectionFrame` → Live/Sequence service consumption path. Each item carries only existing source/event identity and measured source-pixel area. It MUST NOT become an accepted detection or track and MUST NOT carry or create any image/crop, inference, Decision, Observed Route, DAQ, or track-lifecycle state, including when Trigger Every Droplet is ON. Accepted detection/tracking/filtering/`resetFrames` outputs and all qualified detector mechanics remain behavior-compatible; accepted CSV rows write integer `rejected=0`, rejected-observation rows write integer `rejected=1`, and Rejected remains a separate Live/Results count excluded from Total Droplets and all model/Decision/Route counts. Legacy 13-column Run CSV read compatibility remains required. **Exact unchanged implementation write set:** only `app/runtime/fast_event_detector.h` (the existing `FastEventResult` owner), `app/runtime/fast_event_detector.cpp`, `app/runtime/detection/droplet_detector.h` (the existing `DropletDetectionFrame` owner), `app/runtime/detection/droplet_detector_adapters.cpp`, `app/runtime/v2/live/live_sorting_service.cpp`, `app/runtime/v2/sequence_test/sequence_test_service.cpp`, `app/runtime/v2/run/run_manifest_v2.h`, `app/runtime/v2/run/run_manifest_v2.cpp`, `app/runtime/v2/run/run_writer_v2.cpp`, `app/runtime/tests/detector_characterization_test.cpp`, `app/runtime/tests/detector_contract_parity_test.cpp`, `app/runtime/tests/live_sorting_service_test.cpp`, `app/runtime/tests/sequence_test_service_test.cpp`, and `app/runtime/tests/run_manifest_v2_test.cpp` may implement, consume, persist, or verify this fact. The stopped worker's preserved diff remains unchanged until this documentation commit is integrated. No other file, CMake, QML, crop, inference, DAQ, model, callback framework, event bus, new service, registry, schema expansion, generalized abstraction, or per-consumer duplicate contract is authorized; the only persistence addition remains the already-approved integer CSV field `rejected`. Representation MUST be the smallest ordinary C++ collection/buffer contract, reuse capacity or caller-owned storage as appropriate, prove zero steady-state per-frame heap allocation, and keep candidate iteration bounded. Stop if implementation proposes a generalized callback/event bus/new abstraction. **Required deterministic fixture:** one accepted contour coexists with multiple otherwise-valid undersized candidates; accepted output and tracking are identical before/after; every rejected fact is emitted exactly once in deterministic source order; Live/Sequence rows and distinct Live/Results Rejected counts are correct. Retain the existing scoped-review corrections and gates without creating a new decision: legacy 13-column read compatibility, separate counts, before/after characterization, zero image allocation, zero steady-state per-frame heap allocation, bounded replay/performance with no material hot-path regression, zero forbidden downstream outputs, and one scoped independent review. No HIL is required for detector fact exposure; eventual combined DAQ HIL must cover rejected-output zero. Justification is the smallest factual seam needed for approved CSV/count semantics; rollback is revert of the bounded implementation commit. |
| `UAT-REJECT-001-B1` | FUNCTIONAL backend/wrapper/Results correction owner; DESIGN one-form owner; VALIDATION changed-lines rereview owner | Scoped review FINAL REJECT; preserved implementation uncommitted/stopped; exact backend, visual, wrapper, and Results seams authorized | **Exact B1 Live backend expansion:** only `app/runtime/v2/live/live_sorting_service.h`, `app/runtime/v2/live/live_sorting_service.cpp`, `app/runtime/v2/live/live_sorting_controller.h`, `app/runtime/v2/live/live_sorting_controller.cpp`, `app/runtime/tests/live_sorting_service_test.cpp`, and `app/runtime/tests/live_sorting_controller_test.cpp` may expose and verify one authoritative Live rejected count from the existing service to the existing controller. Rejected observations increment it exactly once; accepted events do not. It remains distinct from Total Droplets and all prediction, Decision, and Route counts, and its reset/lifecycle follows the existing Live run counters. The service remains the sole state owner; no duplicate or new owner is authorized. **Exact DESIGN visual write:** only `app/runtime/Desktop_app_v2/Desktop_app_v2Content/LiveWorkspace.ui.qml` may add root `property string rejectedCountText: qsTr("0")` plus a same-style `Rejected: %1` `Text` immediately after the existing Total Droplets text driven by `persistedEventCountText`. Add no alias, state, or new layout. **Exact FUNCTIONAL wrapper write:** only `app/runtime/Desktop_app_v2/Desktop_app_v2Content/ShellSingleImage.qml` may add one `Binding` adjacent to the existing `persistedEventCountText` binding. No other QML is authorized. **Exact Results expansion:** Results requires no form change; the existing `RunsWorkspace` `loadedRunFacts` interface remains unchanged. Only `app/runtime/v2/results/runs_results_controller.h`, `app/runtime/v2/results/runs_results_controller.cpp`, and `app/runtime/tests/runs_results_controller_test.cpp` may include the distinct factual Rejected count in the existing controller-composed `loadedRunFacts`/`predictedCounts`/summary presentation. Do not add a dedicated Results form line or interface. **Required order:** integrate the authority commits; DESIGN produces and reviews one immutable form-only commit; FUNCTIONAL then resumes B1/B2/multi, integrates that form, and performs the wrapper/controller/test corrections; finish with changed-lines rereview only. No speculative UI, other backend/QML/CMake/test file, new state owner, or new interface is authorized. |
| `UAT-REJECT-001-B2` | FUNCTIONAL manifest-loader correction owner; VALIDATION changed-lines rereview owner | Scoped review FINAL REJECT; preserved implementation uncommitted/stopped; exact legacy correction authorized | Without expanding the existing write set, only `app/runtime/v2/run/run_manifest_v2.h`, `app/runtime/v2/run/run_manifest_v2.cpp`, and `app/runtime/tests/run_manifest_v2_test.cpp` may normalize absent legacy `counts.rejected` to integer zero, and only when the **same Run** is verified as authentic legacy by both its old summary contract and its 13-column `events.csv`/log contract. Modern or 14-column Run data missing `counts.rejected` remains invalid; generic defaulting is prohibited. The deterministic fixture must contain both an authentic old summary lacking `counts.rejected` and its authentic 13-column log, and must preserve strict rejection of modern missing-field data. No schema expansion or other compatibility relaxation is authorized. Perform this within the same one correction cycle and changed-lines rereview authorized after B1's exact DESIGN seam publication. |
| `ENV-ONNX-DLL-001` | FUNCTIONAL environment owner; VALIDATION qualification owner | RESOLVED/CLOSED — configuration-root diagnosis; no requalification | Reject defaulted to stale `C:\onnxruntime-gpu` v1.23 DLL SHA `C602...82C`. Accepted builds use the exact existing qualified root `C:\Users\goals\AppData\Local\Temp\opendss-ort-1.25.1-cuda13\extracted\onnxruntime-win-x64-gpu-1.25.1`; its headers/import library and DLL SHA `A64BDD69...83B1B` match the repository lock. Every future Reject configure must pass that exact `ONNXRUNTIME_DIR`. No runtime file or hash changed, and no requalification or repeat is required. This diagnosis remains independent of `UAT-REJECT-001-PROT` and does not waive, replace, downgrade, or stand in for detector characterization, downstream Live/Sequence acceptance, scoped review, or eventual combined DAQ HIL. |
| `UAT-RUNS-001` | User / documentation authority; FUNCTIONAL correction owner | FINAL PASS/CLOSE — integrated and pushed at `4625e28` | The right Runs panel is internally scrollable and keeps its bottom actions reachable. Effective Live and Sequence roots aggregate with identity deduplication. Repository/Shell focused evidence and the scoped rereview passed. |
| `UAT-RUNS-002` | User / documentation authority; FUNCTIONAL correction owner | FINAL PASS/CLOSE — integrated and pushed at `4625e28` | Remove Run acts only after explicit confirmation, then uses native Windows Recycle Bin removal for the complete selected Run folder and all contained files; cancellation changes nothing. Identity-replacement guarding, active-run protection, and containment safety passed focused repository/removal/Shell evidence and scoped rereview. |
| `UAT-RUNS-DESIGN-001` | DESIGN | Separate nonblocking queue | Visual improvement is requested for the Runs panel. No design is inferred by this authority update. |
| `UAT-MODEL-LIB-001` | User / documentation authority; DESIGN exact-form owner after seam inventory; FUNCTIONAL artifact owner | Authority published; workflow queued after corrected-candidate Training freeze acceptance | Under `ODSS-DES-002` §§12.1–12.9 and 14.2–14.9, Library owns Add Model and Import Model. Add Model popup requires nonblank unique Name, `MobileNetV3-Small` or `EfficientNet-B0`, and approved Starting Weights. Train selects that Library-defined identity/architecture/initialization read-only, plus Dataset, Compute Device, and Output Location. Retraining always uses a new Name and atomically publishes a new package only on success; source identity/artifacts remain intact and protected. Import accepts only a complete package selected via `metadata.json`; the documented repo-only converter supports only those two architectures. Remove Model is adjacent to Import Model and, after confirmation, moves the OpenDSS-owned complete package folder to Windows Recycle Bin. Active Model, in-use, Model Registry, and package-integrity locks remain mandatory. DESIGN first receives exact Library/Train protected-form ownership only after seam inventory; FUNCTIONAL then owns authoritative create/import/remove/recycle/locks and Train selection/read-only consumption/atomic publication, followed by single-writer integration. No exact form filename is asserted by the current master/ledger. |
| `UAT-PATH-001` | User / documentation authority; FUNCTIONAL artifact/UI-binding owner; VALIDATION final visible acceptance owner | SOFTWARE FINAL PASS/CLOSED at integrated/pushed `0255b3e`; final combined-candidate visible check retained | `ODSS-DES-002` §8.6 output defaults, per-selector persistence, visible invalid-path fallback, and unique non-overwriting subfolders are implemented and independently reviewed. Settings plus Shell evidence passed 52/52. Do not repeat software validation. The final combined-candidate user-visible check remains governed by `FINAL-UAT-001`; packaging remains separately user-gated. |
| `UAT-PATH-001-RUNS` | FUNCTIONAL existing-preferences and Runs controller/Shell binding/test owner | FINAL PASS/CLOSED with `UAT-RUNS-001/002` at integrated/pushed `4625e28` | `RunsResultsController` aggregates the current effective Live and Sequence Test run roots, scans a shared canonical root once, and deduplicates by canonical Run-folder identity. Focused repository/removal/Shell evidence and scoped rereview passed with existing Run validation/loading plus active/in-use safety preserved. No historical-root registry, recursive global-drive scan, migration, schema, or storage redesign was added. Do not repeat. |
| `PATH-EXEC-001` | FUNCTIONAL test-harness owner; VALIDATION execution acceptance owner | FINAL PASS/CLOSED at integrated/pushed `8ab3827e80d24200386eab4e8ef89eb7cf5cffbd` | The first registered run failed before product assertions because Windows loaded System32 ONNX Runtime 1.17 instead of qualified 1.25; no PATH behavior failed, the audit was clean, and three TEMP fixture directories were preserved as failure evidence. The separately reviewed correction stages only the exact qualified locked `${ONNXRUNTIME_DIR}/lib/onnxruntime.dll` beside `model_library_controller_test` and `training_controller_test`, with configure-time source/hash preflight. The exact registered rerun passed 2/2 in 9.29 s and 7.68 s; log SHA `CCD7E99D…CC466`; qualified and staged DLL SHA `A64BDD69…83B1B`; zero relevant process, window, dialog, or crash residue. PATH source commit `6f1a91e` remains unchanged and FINAL ACCEPT. Do not rerun. |
| `UAT-VIEWER-002` | User / documentation authority; DESIGN owner of `FullSizeImageViewerForm.ui.qml` only; FUNCTIONAL policy-assertion/harness-test owner | Authority published; combined form correction pending; not accepted | Under `ODSS-DES-002` §6.5.1, each axis in the one shared `FullSizeImageViewer` uses scrollbar policy `AlwaysOn` exactly when that axis's content exceeds its viewport and `AlwaysOff` otherwise. Both scrollbars are interactive and remain persistently visible and clickable throughout overflow; transient or `AsNeeded` presentation is prohibited. Preserve normal-wheel vertical scroll, `Shift`+wheel horizontal scroll, pointer-centered `Ctrl`+wheel zoom clamped `0.3`–`10`, pan, all interfaces, and geometry. DESIGN may change only `FullSizeImageViewerForm.ui.qml` in one combined `UAT-VIEWER-002`/`003` correction. FUNCTIONAL may add only policy assertions and perform one changed Shell rerun through the accepted central harness after `VAL-TEST-UI-005` separately releases execution. No GUI repeat, inference, or per-consumer duplication. |
| `UAT-VIEWER-003` | User / documentation authority; DESIGN owner of `FullSizeImageViewerForm.ui.qml` only; FUNCTIONAL policy-assertion/harness-test owner | Authority published; combined form correction pending; not accepted | Under `ODSS-DES-002` §§5.2 and 6.5.1, the shared full-size image-viewer canvas/background is black (`#000000`) for exactly five consumers: Capture, Live, Sequence Test, Sequence Viewer, and Label selected crop. Replace only the former blue/navy canvas. Surrounding panels, grids and thumbnails, placeholder text, overlays, controls, borders, interfaces, and geometry remain unchanged. Ownership and validation are the same single combined form correction and one changed Shell rerun specified by `UAT-VIEWER-002`; no per-consumer copy or additional GUI run is authorized. |
| `SCHED-UAT-002` | FUNCTIONAL single-writer integration owner; DESIGN exact-form owner where required | Queued; release only after corrected-candidate `VAL-TRAIN-CU-001` frozen handoff | After the Training freeze is accepted, release maximum safe nonoverlapping lanes: (1) critical DAQ/route FUNCTIONAL correction, never delayed by visual polish; (2) rejection CSV/detector persistence FUNCTIONAL; (3) Runs scroll/remove plus Runs visual DESIGN→FUNCTIONAL as ownership permits; (4) shared Viewer conditional DESIGN→FUNCTIONAL runtime; (5) Model Library/Train workflow DESIGN→FUNCTIONAL; (6) output defaults FUNCTIONAL. Use separate worktrees and build roots, no overlapping protected forms/files, and single-writer integration in dependency order. Do not assign exact files until each seam inventory is complete. |
| `UAT-ROUTE-003` | FUNCTIONAL correction owner; VALIDATION changed-seam acceptance owner | Factual defect confirmed; protected correction released only through `UAT-ROUTE-003-PROT`; not a DAQ gate | Live and Sequence Test currently finalize a pending event on the first `!detected` frame while the qualified Fast Event Detector retains the track through `resetFrames=2`. This makes Observed Route use an early Y and is independent of Decision-to-DAQ behavior. |
| `UAT-ROUTE-003-PROT` | FUNCTIONAL correction owner; VALIDATION changed-seam acceptance owner | RELEASED after this documentation commit; implementation pending | Controlling authority remains `ODSS-DES-002` §§15.7, 15.13, and 16.4: Observed Route is finalized from the track's final Y; a Running boundary replacement applies immediately to subsequent calculations; boundary state is workspace-local, nonpersistent, and observer-only with zero DAQ effect. **Current consumers/targets before edit:** `FastEventDetector` feeds `FastEventDetectorAdapter` and `IDropletDetector::processFrame`; the affected route consumers are `LiveSortingService` and `SequenceTestService`. Dataset Capture, `PipelineRunner`, and CLI also consume the shared detection result and must remain behaviorally unchanged. Existing build targets are `opendss_runtime` and `Desktop_app_v2App`; focused acceptance targets are `opendss_detector_characterization_test`, `opendss_detector_contract_parity_test`, `live_sorting_service_test`, `live_sorting_controller_test`, `sequence_test_service_test`, and `sequence_test_controller_test`. **D1 / exact authorized write set:** only `app/runtime/fast_event_detector.h`, `app/runtime/fast_event_detector.cpp`, `app/runtime/detection/droplet_detector.h`, `app/runtime/detection/droplet_detector_adapters.cpp`, `app/runtime/v2/live/live_sorting_service.cpp`, `app/runtime/v2/sequence_test/sequence_test_service.cpp`, `app/runtime/tests/detector_characterization_test.cpp`, `app/runtime/tests/detector_contract_parity_test.cpp`, `app/runtime/tests/live_sorting_service_test.cpp`, and `app/runtime/tests/sequence_test_service_test.cpp` may expose and consume the smallest factual track/lifecycle-ended indication. Do not replace or rewrite the detector or change qualified detection, centroid, `resetFrames=2`, crop, inference, performance, or DAQ mechanics. **D2 / exact authorized write set:** only the existing Live/Sequence service and controller headers/implementations plus their focused service/controller tests—`app/runtime/v2/live/live_sorting_service.h`, `app/runtime/v2/live/live_sorting_service.cpp`, `app/runtime/v2/live/live_sorting_controller.h`, `app/runtime/v2/live/live_sorting_controller.cpp`, `app/runtime/v2/sequence_test/sequence_test_service.h`, `app/runtime/v2/sequence_test/sequence_test_service.cpp`, `app/runtime/v2/sequence_test/sequence_test_controller.h`, `app/runtime/v2/sequence_test/sequence_test_controller.cpp`, `app/runtime/tests/live_sorting_service_test.cpp`, `app/runtime/tests/live_sorting_controller_test.cpp`, `app/runtime/tests/sequence_test_service_test.cpp`, and `app/runtime/tests/sequence_test_controller_test.cpp`—may update the current observer Decision Boundary X/Y/side thread-safely during Running so subsequent lifecycle finalizations use the replacement immediately. No CMake, QML, schema, manifest, writer, persistence, export, history, DAQ, or other production file is authorized. **Required gates:** use one representative deterministic fixture to characterize detection → one miss retains the lifecycle/pending event → reappearance updates final Y → second consecutive miss ends the lifecycle and finalizes; cover the same lifecycle timing in focused Live and Sequence tests; cover in-Run X/Y/side replacement and prove the next lifecycle finalization uses the replacement in both services; record before/after factual route/timing comparison; record bounded replay performance and prove no new per-frame allocation or contended per-frame lock. No HIL is required because the correction is observer-only and has no DAQ impact. Justification is compliance with lifecycle-final observer authority; rollback is revert of the bounded implementation commit. Stop before implementation if the interface requires broader detector behavior, any schema/persistence/DAQ change, a file outside this write set, or conflicting ownership. |
| `FINAL-UAT-001` | VALIDATION consolidated GUI/HIL owner; FUNCTIONAL frozen-candidate/fix owner | Prior unchanged evidence preserved; corrected-candidate gate reopened only for `UAT-DAQ-004` | Visible UAT/window, nonblank retention/no-flicker, and unrelated HIL evidence remain accepted without repetition. The old finalized-route DAQ-arbitration acceptance is retracted. Validation remains idle until the authority lock is integrated, FUNCTIONAL corrects `UAT-DAQ-004` with scoped review, and a frozen corrected candidate is released. |
| `SCHED-001` | User acceptance owner; FUNCTIONAL installer owner | Release frozen by `UAT-DAQ-004`; final installer stamp remains blocked | Preserve accepted unrelated evidence without repetition. Release requires the separately reviewed `UAT-DAQ-004` correction and frozen corrected candidate; `UAT-ROUTE-003` and `UAT-RUNS-DESIGN-001` are not DAQ-release gates. The final stamped installer still requires explicit user acceptance; fresh-machine qualification remains deferred. |
| `UAT-MODEL-005` | FUNCTIONAL artifact owner | Provenance authority approved; release green backend `807625a` after master/lock publication | Visible workspace name remains `Model Test`; input is one structured/labeled OpenDSS Dataset. Internal processing uses local Python/PyTorch at `%LOCALAPPDATA%\OpenDSS\training-venv-gpu`, GPU-first with CPU fallback. Trusted package metadata requires `checkpoint_sha256`; legacy local packages missing it migrate automatically/atomically. New `opendss.model_test.v3` summaries record executed checkpoint SHA-256 plus metadata SHA-256; v2 remains readable. Missing/mismatched checkpoint or failed migration blocks execution. Live/Sequence retain ONNX+metadata provenance. No visible UI/action or user batch setting. Preserve per-image order/facts, completed-batch atomic checkpoints, Stop boundary, recovery, and allocation fallback. Reusable backend `807625a` passed 42/42 but remains unconsumed until this authority is integrated. |
| `ASSUMPTION-AUDIT-001` | User / documentation authority | User decisions resolved; implementation seams released after master/lock publication | Model Test naming/input, restored-window units, detector legacy migration/rounding, and Decision Boundary equality behavior are resolved in their owning rows above. No additional product mode, input surface, detector state, physical-pixel gate, or equality fallback may be inferred. |
| `FI-001` | FUNCTIONAL | Automated checks accepted | Production wiring covers Capture, Label, Sequence Viewer, Training, Model Library, Live, Sequence Test, Runs, and Settings. Release GUI: `C:\b\odss-v2-fpath-8435\Release\Desktop_app_v2App.exe`. |
| `CR-001` | FUNCTIONAL | Independent review accepted | `OperationCoordinator` callback invocation and destruction are serialized by a dedicated recursive mutex. The focused test runs 1,000 concurrent release/destruction iterations. |
| `FI-TEST-001` | FUNCTIONAL | Passed | Thirteen affected C++ CTest targets passed 13/13 in `C:\b\odss-v2-sequence-model-8435`; QML shell suite passed 43/43 in `C:\b\odss-v2-fpath-8435`. |
| `FI-MODEL-001` | FUNCTIONAL | Passed | The trusted installed EfficientNet model loaded locally without network fallback and the production probe processed exactly 2,103 frames with DAQ off. |
| `FI-REVIEW-001` | Reviewer | Accepted | Initial three blockers, `CR-001`, camera identity/continuous Stop, `VAL-DAQ-003`, and `VAL-GUI-003` received scoped read-only acceptance. |
| `VG-001` | VALIDATION | Candidate passed; immutable hash pending | Maximized full-area validation at 1707 × 1019 passed factual camera identity, visible frames during streaming, and the corrected DAQ arbitration presentation. Final acceptance awaits the pushed correction hash. |
| `HIL-001` | VALIDATION | Passed for corrected candidate | Real `DCAM:0` streaming preview passed. Real `Dev1/ao0` continuous 5 Vpp/10 kHz Start/Stop/exit passed with exact-zero regression evidence; the competing finite-test action is visibly disabled during continuous ownership. |
| `VAL-DAQ-003` | FUNCTIONAL / VALIDATION | Corrected, reviewed, HIL passed | Live `Send Test Sine Wave` is disabled while continuous output owns DAQ. QML regression and maximized real-HIL evidence pass. |
| `VAL-GUI-003` | FUNCTIONAL / VALIDATION | Corrected, reviewed, HIL passed | Camera acquisition remains full-rate while preview URL publication is bounded to 250 ms so asynchronous QML loads complete. Focused controller evidence and maximized real-DCAM visible-frame validation pass. |

## Delivered behavior

- Production GUI controller and context wiring replaces mock-only runtime paths.
- Camera profiles support asynchronous acceptance/readback; capture uses a global
  workflow gate and production collection/dataset roots.
- Sequence Viewer preserves native 1:1 frame dimensions.
- Training and Model Library enforce trusted local weight/checkpoint metadata and
  suppress production mock rows and mock empty/error data.
- Live scheduling, finite immediate DAQ test output, preserved delayed event
  output, continuous hardware Start/Stop, suppression, and stop-to-zero are
  separated and covered by focused fake-output tests.
- Configuration displays the factual production camera controller
  identity instead of an illustrative fallback.
- Live camera acquisition retains every latest frame while preview URL updates
  are bounded so a visible asynchronous preview remains present during streaming.
- Continuous DAQ Stop, fault, and exit teardown release operation ownership only
  after unlocking DAQ state. Focused tests prove re-entrant notification safety,
  task Stop/clear, and an exact scalar zero write for Stop and exit.
- Competing finite DAQ test output is visibly unavailable while continuous output
  owns the DAQ resource.
- Sequence Test completion/recovery, production Runs notes, Settings diagnostics,
  and native folder opening are wired.

## UAT authority-first continuation

`UAT-CONFIG-001` and `UAT-VIEWER-001` remain approved bounded slices. `UAT-ROUTE-002` route-semantic acceptance is superseded/retracted by `UAT-DAQ-004`; only its applicable general safety evidence remains accepted. Publication of the matching `ODSS-DES-002` and consolidated lock does not itself modify production, QML, generated, detector, routing, viewer, or persistence code.

After publication:

1. DESIGN may receive exact protected-form ownership for Configuration, Decision Boundary, and viewer presentation only through nonoverlapping exact work orders.
2. FUNCTIONAL remains accountable for the runnable artifact and integrates the smallest direct paths to the existing detector threshold, model-Decision-to-DAQ owner, observer-only Route calculation, and shared full-size viewer presentation.
3. No worker may add width/height rejection, a second detector state, Run provenance for in-run threshold changes, boundary coordinate persistence/history, detector-mechanics replacement, a second reference-line feature, thumbnail zoom, or duplicated hot-path viewer JavaScript.

## Protected boundaries

- Preserve qualified DCAM, NI-DAQmx, detector/crop, ONNX, trainer, export, and
  persistence mechanics except for the bounded stabilization changes above.
- Do not rebuild datasets, introduce network fallback, accept untrusted weights,
  weaken checkpoint metadata validation, or restore production mock data.
- Do not broaden `CR-001` into a notification framework or Live service refactor.
- Integration remains the sole writer; Validation and review remain read-only.

## Next gate

Push the exact reviewed correction, hand its immutable commit hash and unchanged
executable path to Validation, and close the slice after `VG-001` records final
acceptance against that hash.
